package net.caffeinemc.gfx.api.texture;

/**
 * A sampler object stores the parameters for how a shader should sample from a texture. It is similar to a Vertex
 * Array in the sense that it specifies how data should be read by the graphics device, except a Sampler is used for
 * texture data rather than vertex attribute data.
 */
public interface Sampler {
}
